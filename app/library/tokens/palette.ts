// if we ever want to support dark mode, we'll need to create a global context for these
export class Palette {
  static readonly PRIMARY = "#006400";
  static readonly PRIMARY_DARK = "#002400";
  static readonly FONT_LIGHT = "#fff";
  static readonly FONT_DARK = "#333";
}
